# Copy to .env.local and fill in. .env.local is gitignored.

# ── Browser (Vite) ──────────────────────────────────────────────
VITE_SUPABASE_URL=https://YOUR-PROJECT-REF.supabase.co
VITE_SUPABASE_ANON_KEY=eyJ...your-anon-key...

# ── Server-side only (Vercel API routes / cron) ─────────────────
# These should ALSO be set in Vercel → Settings → Environment Variables.
# DO NOT prefix with VITE_; they must not reach the browser.
SUPABASE_URL=https://YOUR-PROJECT-REF.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJ...your-service-role-key...

# Gmail credentials are per-user — each user configures their own
# Gmail address + App Password in the in-app Settings page.
# No global GMAIL_* env vars are needed.
